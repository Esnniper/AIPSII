// let salario, dias, salario_diario

// salario = prompt("Digite seu salario: ")
// salario = Number(salario)

// dias = Number(prompt("quantos dias: "))

// salario_diario = salario / dias 

// alert("Salrio diário R$" + salario_diario.toFixed (2))


// let conta, cafe, coxinha, halls, doceDeLeite

// cafe = prompt("Preço do café")
// cafe = Number(cafe)
// coxinha = prompt("Preço da coxinha ")
// coxinha = Number(coxinha)
// halls = prompt("Preço do halls")
// halls = Number(halls)
// doceDeLeite = prompt("Preço do Doce de Leite")
// doceDeLeite = Number(doceDeLeite)

// conta = cafe + coxinha + halls + doceDeLeite

// alert("Sua conta deu R$" + conta.toFixed(2))


// let media, nota1, nota2, nota3, nota4

// nota1 = prompt("qual é primeira nota?")
// nota1 = Number(nota1)
// nota2 = prompt("Qual é a segunada nota?")
// nota2 = Number(nota2)
// nota3 = prompt("Qual é a terceira nota ?")
// nota3 = Number(nota3)
// nota4 = prompt("Qual é a quarta nota ? ")
// nota4 = Number(nota4)

// media= (nota1 + nota2 + nota3 + nota4) / 4 

// alert("  Sua nota final foi  " + media.toFixed(2) )


// let pontos, vitorias, empates 

// vitorias = prompt("Quantas vitorias" )
// vitorias = Number(vitorias)

// empates = prompt("Quantos empates ")
// empates = Number(empates)

// pontos = (vitorias *3) + empates

// alert("Sua pontuação final foi "+ pontos)

// let cafe, coxinha,  halls, doceDeLeite, conta 
 
// cafe = prompt("Preço do café")
//  cafe = Number(cafe)
// coxinha = prompt("Preço da coxinha ")
//  coxinha = Number(coxinha)
//  halls = prompt("Preço do halls")
//  halls = Number(halls)
// doceDeLeite = prompt("Preço do Doce de Leite")
//  doceDeLeite = Number(doceDeLeite)
 
//  conta = ( 2* cafe) + coxinha + halls + doceDeLeite

//  alert("Sua conta deu R$" + conta.toFixed(2))

//  let dano, danoCritico 

// dano = prompt("O dano que você deu no personagem ")
// dano = Number(dano )

// danoCritico = dano * 1.5 
// alert("Seu dano critico foi de" + danoCritico)

// let dano, danoCritico , bonus 

// dano = prompt("O dano do personagem")
// dano = Number(dano)

// bonus = prompt("Mais o bonus do seu personagem ")
// bonus = Number(bonus)

// danoCritico = (1.5* dano) + bonus 

// alert (" Seu dano critico mais o bonus" +  danoCritico)

// let media, n1, p1, n2, p2 

// n1 = prompt  ("Sua nota 1")
// n1 = Number(n1)

// p1 = prompt("Sua nota ponderada  1 ")
// p1 = Number(p1)

// n2 = prompt("Sua nota 2")
// n2 = Number (n2)

// p2 = prompt("Sua nota ponderada 2")
// p2 = Number(p2)


// media = ((n1 * p1) + (n2 * p2)) / (p1 + p2)
// alert("Média: " + media.toFixed(1))

// let poupança, salario, água, netflix , gasolina, luz, internet, outros, moradia 

// salario = prompt("Seu salario ")
// salario = Number(salario)

// moradia = prompt("sua moradia ")
// moradia = Number( moradia)

// água = prompt("Sua água ")
// água = Number(água)

// netflix = prompt("Sua netflix")
// netflix = Number(netflix)

// gasolina = prompt("Sua gasolina ")
// gasolina = Number(gasolina)

// luz = prompt("Sua luz")
// luz = Number(luz)

// internet = prompt("Sua internet")
// internet = Number(internet)

// outros = prompt("Outros")
// outros = Number(outros)

// poupança = (salario - (moradia + internet + netflix + água + gasolina + luz + gasolina + outros ))

// alert ("Sua poupança: " + poupança.toFixed(1))